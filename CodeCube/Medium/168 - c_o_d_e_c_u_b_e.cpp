#include <bits/stdc++.h>
using namespace std;
char a[100100];
int main(){
    int i,len;
    scanf(" %s",a);
    len = strlen(a);
    for(i=0;i<len;i++){
        if(a[i]=='c' && a[i+1]!='c')
    }
    return 0;
}
